package com.example.unimarket.model.api
/*
//import com.example.unimarket.model.entity.Delivery
import retrofit2.Response
import retrofit2.http.GET
import retrofit2.http.Header
import retrofit2.http.Query

interface DeliveriesApi {

    @GET("rest/v1/deliveries")
    suspend fun list(
        @Query("select")
        select: String = "id,address_delivery,created_at",
        @Query("address_delivery") addressNotNull: String = "not.is.null",
        @Query("order") order: String? = null,
        @Query("limit") limit: Int? = 50,
        @Header("Prefer") prefer: String = "count=estimated"
    ): Response<List<String>>
}*/